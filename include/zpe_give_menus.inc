#if defined _zpe_give_menus_included

  #endinput

#endif

#define _zpe_give_menus_included

/**
 * Shows available give menus for player and enables them
 * back again if automatic weapon selection was enabled.
 *
 * @param {player} Player index.
 *
 * @return None.
 */
native zpe_give_menus_show(player);